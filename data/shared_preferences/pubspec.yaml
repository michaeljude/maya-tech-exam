name: data_shared_preferences
description: "A new Flutter package project."
version: 0.0.1

publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.5.3