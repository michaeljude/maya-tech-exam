name: data_flutter_secure_storage
description: 'A new Flutter package project.'
version: 0.0.1
publish_to: 'none'

resolution: workspace

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  domain:
    path: ../../domain
  flutter_secure_storage: ^9.2.4
  logging: ^1.3.0
