name: data_json_serializable
description: "A new Flutter package project."
version: 0.0.1

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.8.1 <4.0.0'
  flutter: '3.32.1'

dependencies:
  flutter:
    sdk: flutter
  json_serializable: ^6.9.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
